<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Categories</h2>
    <ul>

        <% categories.forEach(categories => { %>
        <li><%= categories.name %></li>

        <% }); %>
    </ul>

</main>

<%- include('partials/footer') %>